[pytest]
# Collect only the real test suite. `scripts/` holds operational one-off scripts
# named test_*.py (e.g. test_order_placement.py) that execute on import and are
# not pytest tests — collecting them breaks a bare `pytest` run.
testpaths = tests
